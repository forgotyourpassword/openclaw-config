# 2026-03-24

- Telegram bot setup completed and token was rotated.
- Gmail skill was configured to use `MATON_API_KEY` through OpenClaw secrets.
- Workspace repo was connected to GitHub repo `forgotyourpassword/openclaw-config`.
- Long-term Git auth for workspace repo uses SSH host alias `github-openclaw-workspace` with dedicated deploy key `~/.ssh/openclaw_workspace_github`.
- `.clawhub/` is ignored in the workspace repo.
