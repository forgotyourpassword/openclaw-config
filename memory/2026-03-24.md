# 2026-03-24

- Telegram bot setup completed and token was rotated.
- Gmail skill was configured to use `MATON_API_KEY` through OpenClaw secrets.
- Workspace repo was connected to GitHub repo `forgotyourpassword/openclaw-config`.
- Long-term Git auth for workspace repo uses SSH host alias `github-openclaw-workspace` with dedicated deploy key `~/.ssh/openclaw_workspace_github`.
- `.clawhub/` is ignored in the workspace repo.
- Obsidian vault path is `/home/mike/vault`.
- Vault repo should use SSH host alias `github-openclaw-vault` with dedicated deploy key `~/.ssh/openclaw_vault_github`.
- When making changes to the Obsidian vault for Mike, proactively offer to commit and push the vault repo so it stays backed up.
- Designed and created a `Home/` project-tracking system inside the Obsidian vault with `Dashboard.md`, `Decisions.md`, `Maintenance.md`, folders for active/someday/done projects, areas, contractors, purchases, and Templater-friendly templates.
- The home-project system should track `Priority`, `Urgency`, `Level of effort`, `Cost`, and `Time estimate` so future planning questions like "what can I do Saturday morning?" can be answered.
- Seeded the vault with an active project: kids bathroom drywall repair from a toilet paper holder pulling out, linked to `Home/Areas/Area - Kids Bathroom.md`.
